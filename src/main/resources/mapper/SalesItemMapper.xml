<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SalesItemMapper">

	<!-- 공통 WHERE -->
	<sql id="baseWhereProduct">
		<where>
			<!-- 기간 -->
			<if test="startDate != null and startDate != ''">
				and si.createdat &gt;= str_to_date(#{startDate}, '%Y-%m-%d')
			</if>
			<if test="endDate != null and endDate != ''">
				and si.createdat &lt;= str_to_date(concat(#{endDate}, ' 23:59:59'), '%Y-%m-%d %H:%i:%s')
			</if>

			<!-- 카테고리 -->
			<if test="categoryIds != null and categoryIds.size() &gt; 0">
				and p.codebid in
				<foreach collection="categoryIds" item="cid" open="("
					separator="," close=")">
					#{cid}
				</foreach>
			</if>
			<if test="categoryName != null and categoryName != ''">
				and cb.name = #{categoryName}
			</if>

			<!-- 직원(사번/이메일) -->
			<if test="empNum != null and empNum != '' and empNum != 0">
				and si.empnum = #{empNum}
			</if>
			<if test="empEmail != null and empEmail != ''">
				and e.empemail like concat('%', #{empEmail}, '%')
			</if>

			<!-- 상품명 키워드 -->
			<if test="productNameKeyword != null and productNameKeyword != ''">
				and p.name like concat('%', #{productNameKeyword}, '%')
			</if>

			and si.status = 'ACTIVE'
		</where>
	</sql>

	<!-- 카운트 -->
	<select id="selectSalesItemCount" parameterType="map"
		resultType="int">
		select count(1)
		from sales_item si
		join product p on si.productid = p.productid
		join codeb cb on p.codebid = cb.codebid
		left join employee e on si.empnum = e.empnum
		<include refid="baseWhereProduct" />
	</select>

	<!-- 목록 -->
	<select id="selectAllSalesItems" parameterType="map"
		resultType="com.example.gymerp.dto.SalesItemDto">
		select
		si.itemsalesid,
		si.productid,
		p.name as productname,
		cb.name as categoryname,
		si.producttype,
		si.empnum,
		e.empname as empname,      <!-- ✅ 판매자 이름으로 변경 -->
		si.quantity,
		si.unitprice,
		si.totalamount,
		si.status,
		si.createdat,
		si.updatedat
		from sales_item si
		join product p on si.productid = p.productid
		join codeb cb on p.codebid = cb.codebid
		left join employee e on si.empnum = e.empnum
		<include refid="baseWhereProduct" />
		order by si.createdat desc, si.itemsalesid desc
		limit #{endRow} offset #{startRow}
	</select>

	<!-- 단건 상세 -->
	<select id="selectSalesItemById" parameterType="long"
		resultType="com.example.gymerp.dto.SalesItemDto">
		select
		si.itemsalesid,
		si.productid,
		p.name as productname,
		cb.name as categoryname,
		si.producttype,
		si.empnum,
		e.empname as empname,        <!-- ✅ 판매자 이름으로 변경 -->
		si.quantity,
		si.unitprice,
		si.totalamount,
		si.status,
		si.createdat,
		si.updatedat
		from sales_item si
		join product p on si.productid = p.productid
		join codeb cb on p.codebid = cb.codebid
		left join employee e on si.empnum = e.empnum
		where si.itemsalesid = #{value}
	</select>

	<!-- 조정용 조회 -->
	<select id="selectSalesItemForAdjustment" parameterType="long"
		resultType="map">
		select
		si.productid,
		si.quantity as oldquantity,
		p.codebid as codebid,
		si.status
		from sales_item si
		join product p on si.productid = p.productid
		where si.itemsalesid = #{value}
	</select>

	<!-- 등록 -->
	<insert id="insertSalesItem"
		parameterType="com.example.gymerp.dto.SalesItemDto" useGeneratedKeys="true" keyProperty="itemSalesId">
		insert into sales_item (
		productid, productname,
		empnum, quantity, unitprice, totalamount,
		producttype, status, createdat, updatedat
		) values (
		#{productId}, #{productName},
		#{empNum}, #{quantity}, #{unitPrice, jdbcType=DECIMAL}, #{totalAmount,
		jdbcType=DECIMAL},
		#{productType}, #{status}, #{createdAt}, #{updatedAt}
		)
	</insert>

	<!-- 수정 -->
	<update id="updateSalesItem"
		parameterType="com.example.gymerp.dto.SalesItemDto">
		update sales_item
		<set>
			<if test="productId != null and productId != 0">
				productid = #{productId},
			</if>
			<if test="productName != null">
				productname = #{productName},
			</if>
			<if test="empNum != null and empNum != 0">
				empnum = #{empNum},
			</if>
			<!-- quantity는 0 허용 -->
			<if test="quantity != null">
				quantity = #{quantity},
			</if>
			<if test="unitPrice != null">
				unitprice = #{unitPrice, jdbcType=DECIMAL},
			</if>
			<if test="totalAmount != null">
				totalamount = #{totalAmount, jdbcType=DECIMAL},
			</if>
			<if test="productType != null">
				producttype = #{productType},
			</if>
			<if test="status != null">
				status = #{status},
			</if>
			updatedat = now()
		</set>
		where itemsalesid = #{itemSalesId}
	</update>

	<!-- 환원용 구매 등록 -->
	<insert id="insertPurchaseForRefund" parameterType="map" useGeneratedKeys="true" keyProperty="purchaseId">
		insert into purchase (
		productid, codebid, createdat, quantity, notes
		) values (
		#{productId},
		#{codeBId},
		now(),
		#{quantity},
		'판매 취소로 인한 재고 환원'
		)
	</insert>

	<!-- 소프트 삭제 -->
	<update id="deleteSalesItem" parameterType="long">
		update sales_item
		set status = 'DELETED', updatedat = now()
		where itemsalesid = #{value}
	</update>

	<!-- 분석 -->
	<select id="selectItemSalesAnalytics" parameterType="map"
		resultType="map">
		select
		si.productid,
		p.name as productname,
		sum(si.quantity) as totalquantity,
		sum(si.totalamount) as totalsalesamount
		from sales_item si
		join product p on si.productid = p.productid
		<include refid="baseWhereProduct" />
		group by si.productid, p.name
		order by totalsalesamount desc
	</select>

</mapper>
